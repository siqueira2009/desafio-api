// Função para criar usuário
export function userModel(id, name, band) {
    // Retorna um objeto com ID, nome e banda
    return({
        "id": id,
        "name": name,
        "banda": band
    })
}